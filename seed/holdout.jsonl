{"signature": "sitrep checks :: fail: checks (0.0s)", "diagnosis": "Harness artifact, not a product regression: FAIL reported with 0.0s elapsed and no executed-test count means the runner never actually ran (output swallowed by wrapper or wrong invocation). Re-run with native stream merge (cmd /c ... 2>&1) and read the 'Ran N tests' tail before diagnosing product code."}
{"signature": "test_load_config :: jsondecodeerror: expecting property name enclosed in double quotes", "diagnosis": "File begins with a UTF-8 BOM and json.loads chokes on the invisible prefix. Read with encoding='utf-8-sig' so the BOM is stripped before parsing; every reader in this repo uses utf-8-sig for exactly this reason."}
{"signature": "preflight installer probe :: sha256 mismatch after download", "diagnosis": "Stale-installer custody break: what got probed was not the artifact we downloaded (no SHA256 quoted in transcript before probing). Rule R3: always quote the artifact SHA256 in-transcript immediately after download, before any probe or install."}
{"signature": "repocheck scan :: fatal: not a git repository (or any of the parent directories)", "diagnosis": "Tooling invoked outside a git work tree: cwd has no .git up the parent chain (often an empty/uninitialized checkout dir). Verify the working directory is inside the target repo (or pass an explicit root) before running git plumbing; classify as environment error, not a product bug."}
